<% provide(:title, 'Категории') %>

<div class="breads" xmlns="http://www.w3.org/1999/html"><%= link_to "Back office", admin_path %>&nbsp;&rsaquo;&nbsp;Категории</div>

<h3>Категории</h3>
<div class="row">

  <div class="span6">
    <div class="one-new">
      <% @categories.each do |c| %>
          <h4><%= c.name %></h4>

          <p>
            <%= link_to 'Изменить', edit_manage_category_path(c) %>
          </p>


      <% end %>

    </div>
  </div>
</div>
<p>
  <%= link_to 'Новая категория', new_manage_category_path %>
</p>
